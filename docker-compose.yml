services:
  nuxt:
    build: .
    restart: unless-stopped
    ports:
      - "3002:3002"
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
    networks:
      - shared_network

networks:
  shared_network:
    external: true
    name: apps-dragonflydb-zgbv7v