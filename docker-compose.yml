services:
  nuxt:
    build: .
    restart: unless-stopped
    ports:
      - "3002:3002"
    environment:
      - REDIS_HOST=dragonflydb
      - REDIS_PORT=6379
    networks:
      - shared_network

networks:
  shared_network:
    external: true
    name: apps-dragonflydb-zgbv7v